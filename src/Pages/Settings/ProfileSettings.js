import React from 'react';
import '../Page.css'; // Import the CSS file

const ProfileSettings = () => {
  return (
    <div className="Page">
      <div className="dashboard-card1">
        <h2>Welcome to the Profile Settings...</h2>
        <p>Here user can manage all website related profile information efficiently.</p>
      </div>
    </div>
  );
};

export default ProfileSettings;